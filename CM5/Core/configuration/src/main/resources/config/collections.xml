<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="https://cm5.intertrust.ru/config">
    <collection name="Employees" idField="id" domain-object-type="Employee">
        <!--todo поддержка конвертации значений, например, в картинки -->
        <display>
            <column field="id" name="id"type="integer" hidden="true" sortable="false" sequence="0" />
            <column field="business_object_type" name="type" type="string" editable="false"  hidden="true" sortable="false" sequence="1" /> <!--todo - rethink?-->
            <column field="name" name="Имя" editable="false" type="string" sortable="true"  sequence="2" />
            <column field="position" name="Должность" type="string" editable="false" sortable="true"  sequence="3" />
            <column field="created_date" name="Дата создания" type="datetime" editable="false" sortable="true"  sequence="4" />
            <column field="updated_date" name="Дата модификации" type="datetime" editable="false" sortable="true"  sequence="5" />
        </display>

        <prototype>
            <![CDATA[
                select
                    e.id, e.name, e.position, e.created_date, e.updated_date
                from
                    employee e
                     ::from-clause
                where
                    ::where-clause
            ]]>
        </prototype>

        <!-- если запрос-счётчик отсутствует, то в select подставляется count(*) вместо названий колонок -->
        <counting-prototype>
            select count(*) from employee e ::from-clause WHERE ::where-clause
        </counting-prototype>

        <filter name="byDepartment">
            <reference placeholder="from-clause">
                 <![CDATA[
                    inner join department d on e.department = d.id
                ]]>
            </reference>

            <!-- по умолчанию and, если перед условием нет других (только ключ. слово where), то условие опускаем -->
            <!-- {0} - это параметр фильтра, у одного фильтра может быть несколько параметров -->
            <criteria placeholder="where-clause" condition="and">
                <![CDATA[
                    d.name = {0}
                ]]>
            </criteria>
        </filter>

        <filter name="byName">
            <criteria placeholder="where-clause" condition="and">
                <![CDATA[
                    e.name = {0}
                ]]>
            </criteria>
        </filter>
    </collection>

    <collection name="Employees_2" idField="id" domain-object-type="Employee">
        <display>
            <column field="id" name="id" type="integer" hidden="true" sortable="false" sequence="0" />
            <column field="business_object_type" name="type" type="string" editable="false"  hidden="true" sortable="false" sequence="1" /> <!--todo - rethink?-->
            <column field="name" name="Имя" type="string" editable="false" sortable="true"  sequence="2" />
            <column field="position" name="Должность" type="string" editable="false" sortable="true"  sequence="3" />
            <column field="created_date" name="Дата создания" type="string" editable="false" sortable="true"  sequence="4" />
            <column field="updated_date" name="Дата модификации" type="string" editable="false" sortable="true"  sequence="5" />
        </display>
        <renderer className="ru.intertrust.cm.core.business.impl.EmployeesCollectionRenderer"/>
        <filter name="byName"/>
        <filter name="byDepartment"/>
    </collection>
</configuration>
